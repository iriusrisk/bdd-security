<?xml version="1.0" encoding="ISO-8859-1" ?>
<web-app>
    <!-- The settings in this file are for a fictional web application called "ISpatula", you'll need to modify them for your app! -->
    
	<!-- The HttpAware WebDrivers drivers to use, available drivers can be found in net.continuumsecurity.web.driver.HttpAware*Driver -->
	<defaultDriver>BurpHtmlUnit</defaultDriver>
	
	<!-- The driver that the automated tests will use to point at Burp, available drivers can be found in net.continuumsecurity.web.driver.Burp*Driver -->
	<burpDriver>BurpHtmlUnit</burpDriver>
	
	<!-- Base URL for the application to test -->
	<baseUrl>http://localhost:9110/ropeytasks/</baseUrl>
	
	<!-- The fully qualified class name representing the application under test -->
	<class>net.continuumsecurity.examples.ropeytasks.RopeyTasksApplication</class>

	<!-- Names of the session ID cookies -->
	<sessionIds>
		<name>JSESSIONID</name>
	</sessionIds>
	
	<users>
		<user username="bob" password="password">
				<role>user</role>
		</user>
		<user username="alice" password="password">
				<role>user</role>
		</user>
		<user username="admin" password="password">
				<role>admin</role>
				<role>user</role>
		</user>
	</users>

	<!-- The settings below usually don't need to be changed -->
    <burp>
		<host>127.0.0.1</host>
		<port>8080</port>
		<WSUrl>http://127.0.0.1:8181/</WSUrl>
		<WSProxyHost>127.0.0.1</WSProxyHost>
		<WSProxyPort>8086</WSProxyPort>

		
			<policy name="command.injection" enabled="false"/>
			<policy name="xss" enabled="true"/>
			<policy name="misc" enabled="false"/>
			<policy name="xml.injection" enabled="false"/>
			<policy name="ldap.injection" enabled="false"/>
			<policy name="sql.injection" enabled="false"/>
			<policy name="sql.injection.timing" enabled="false"/>
			<policy name="rest.params" enabled="false"/>
			<policy name="all.injection.points" enabled="true"/>
			<policy name="passive" enabled="true"/>
		
	</burp>
	<storyDir>src/main/stories/</storyDir>
	<reportsDir>reports</reportsDir>
	<latestReportsDir>target/jbehave/</latestReportsDir>
</web-app>